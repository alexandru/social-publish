@username = admin
@password = parola
@baseUrl = http://localhost:3000

###
# @name Login
POST {{baseUrl}}/api/login
Content-Type: application/x-www-form-urlencoded

username={{encodeURIComponent(username)}}&password={{encodeURIComponent(password)}}

{{
  $global.jwt=response.parsedBody.token;
}}

###
# @name Feed post
POST {{baseUrl}}/api/feed/post
Authorization: Bearer {{$global.jwt}}
Content-Type: application/json

{
  "targets": ["feed"],
  "language": "en",
  "messages": [
    {
      "content": "Hello from the feed API",
      "link": "https://example.com"
    }
  ]
}

###
# @name Broadcast thread (Bluesky + Mastodon + Twitter + Feed)
POST {{baseUrl}}/api/multiple/post
Authorization: Bearer {{$global.jwt}}
Content-Type: application/json

{
  "targets": ["bluesky", "mastodon", "twitter", "feed"],
  "language": "en",
  "messages": [
    { "content": "Thread root post" },
    { "content": "Thread reply #1" },
    { "content": "Thread reply #2", "link": "https://alexn.org/blog/2026/03/06/copyright-is-dead/" }
  ]
}

###
# @name Broadcast LinkedIn with one follow-up comment
POST {{baseUrl}}/api/multiple/post
Authorization: Bearer {{$global.jwt}}
Content-Type: application/json

{
  "targets": ["linkedin", "feed"],
  "language": "en",
  "messages": [
    { "content": "Just a test, please ignore", "link": "https://alexn.org/blog/2026/03/05/boring-tech-irrelevant/" },
    { "content": "More content...", "link": "https://alexn.org/blog/2026/03/06/copyright-is-dead/" }
  ]
}

###
# @name Upload image
POST {{baseUrl}}/api/files/upload
Authorization: Bearer {{$global.jwt}}
Content-Type: multipart/form-data; boundary=WebKitFormBoundary

--WebKitFormBoundary
Content-Disposition: form-data; name="altText"

My cat
--WebKitFormBoundary
Content-Disposition: form-data; name="file"; filename="cat.jpeg"
Content-Type: image/jpeg

< ./docs/cat.jpeg
--WebKitFormBoundary--

{{
  $global.imageUuid=response.parsedBody.uuid;
}}

###
# @name Post with image in messages[0]
POST {{baseUrl}}/api/multiple/post
Authorization: Bearer {{$global.jwt}}
Content-Type: application/json

{
  "targets": ["bluesky", "mastodon", "twitter", "feed"],
  "messages": [
    {
      "content": "Picture post",
      "images": ["{{$global.imageUuid}}"]
    }
  ]
}

###
# @name Upload second image
POST {{baseUrl}}/api/files/upload
Authorization: Bearer {{$global.jwt}}
Content-Type: multipart/form-data; boundary=WebKitFormBoundary

--WebKitFormBoundary
Content-Disposition: form-data; name="file"; filename="cat2.jpeg"
Content-Type: image/jpeg

< ./docs/cat2.jpeg
--WebKitFormBoundary--

{{
  $global.imageUuid2=response.parsedBody.uuid;
}}

###
# @name Broadcast thread with link in follow-up
POST {{baseUrl}}/api/multiple/post
Authorization: Bearer {{$global.jwt}}
Content-Type: application/json

{
  "targets": ["bluesky", "mastodon", "twitter", "feed"],
  "language": "en",
  "messages": [
    { "content": "Root message — no link here" },
    { "content": "Follow-up with a link", "link": "https://example.com/highlighted" }
  ]
}

###
# @name Broadcast thread with images on both messages
POST {{baseUrl}}/api/multiple/post
Authorization: Bearer {{$global.jwt}}
Content-Type: application/json

{
  "targets": ["linkedin"],
  "messages": [
    {
      "content": "Root message with an image",
      "images": ["{{$global.imageUuid}}"]
    },
    {
      "content": "Follow-up message with another image",
      "images": ["{{$global.imageUuid2}}"]
    }
  ]
}
